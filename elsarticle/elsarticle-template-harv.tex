\documentclass[preprint,12pt,authoryear]{elsarticle}

%% Essential Packages
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{subcaption} % Required for subfigure environment
\usepackage[utf8]{inputenc} % Required for accents (ç, ã, é)
\usepackage{booktabs}
\usepackage{multirow} % Required for multirow cells in tables
\usepackage{lipsum}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}

\journal{Expert Systems with Applications} 

\begin{document}

\begin{frontmatter}

%% Title
\title{Enhancing Metaheuristics for Pistachio Supply Chain Optimization: A Variable Neighborhood Search Algorithm with Problem-Specific Operators}

% Authors
% We map authors to affiliations using labels [aff1, aff2]

\author[aff1]{Raissa Gonçalves Diniz}
\ead{raissagd@ufmg.br}

\author[aff1,aff2]{André Costa Batista}
\ead{andre-costa@ufmg.br}

%% Affiliations

%% Affiliation 1
\affiliation[aff1]{organization={Operations Research and Complex Systems Laboratory, Universidade Federal de Minas Gerais},
            country={Brazil}}

%% Affiliation 2
\affiliation[aff2]{organization={Department of Electrical Engineering, Universidade Federal de Minas Gerais},
            addressline={Av. Antônio Carlos 6627},
            city={Belo Horizonte},
            postcode={31270-010},
            state={Minas Gerais},
            country={Brazil}}

%% Abstract
\begin{abstract}
Efficient supply chain management is critical for high-value agricultural products like pistachios. However, while mixed-integer linear programming models exist for this domain, 
the literature lacks efficient heuristic approaches with well-defined solution methodologies. To address this gap, we present an enhanced Variable Neighborhood Search (VNS) approach for pistachio 
supply chain optimization. We adopt the priority-based representation established in the network design literature, instantiated as an integer permutation, and contribute the
complete specification of the decoding algorithm that maps it onto a feasible transportation tree, a step left implicit in prior work on this problem. Building on this representation,
we develop domain-specific neighborhood structures that exploit the active/inactive facility distinction induced by the decoder to improve search convergence beyond classical operators.
Through comprehensive benchmarking against Genetic Algorithms (GA), Iterated Local Search (ILS), and an exact solver, we demonstrate that the proposed VNS 
outperforms other metaheuristics on medium and large-scale instances, and remains within a small margin of the best bound available from the exact solver in scenarios where the latter becomes computationally intractable.
\end{abstract}

%% Keywords
\begin{keyword}
% Reviewer~4, comment~8: "Sustainable Logistics" was removed. The objective function minimises facility, production
% and transportation costs only; no environmental or social indicator is optimised, so the keyword overstated the
% scope of the model. By-product recovery is what the model actually represents.
Supply Chain Network Design \sep Metaheuristics \sep Variable Neighborhood Search \sep Priority-Based Encoding \sep By-Product Recovery
\end{keyword}

\end{frontmatter}

%% Main Text

% ----------------------------------------------------------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}

The growing role of agriculture in the economic development of many countries highlights the importance of integrating this sector with 
other industries to stimulate overall economic growth. Agriculture not only supports food security but also acts as a major contributor 
to exports, generating significant income and strengthening other sectors. Among various agricultural products, pistachios stand out due to 
their high economic and nutritional value. Major producers such as the US, Turkey, and Iran dominate global pistachio production, 
remaining the leading producers worldwide in 2022 and 2023 \citep{statista2023}. As a result, the strategic importance of pistachios in international trade continues to grow.

With this increasing global demand for pistachios, the need for efficient supply chain networks becomes more critical. Proper supply chain 
management is vital for maintaining product quality, minimizing environmental impacts, and ensuring the smooth flow of goods from production 
to consumers. A key challenge in pistachio production is the management of its by-products, such as hulls and shells, which, while free of heavy 
metals or pathogens, can degrade soil quality and contribute to environmental pollution if not properly handled \citep{doula2014, bartzas2017}. 
Furthermore, improper disposal of these materials may lead to the growth of molds that increase the risk of aflatoxin contamination, posing serious 
health risks such as liver cancer \citep{hokmabadi2018,dronavalli2019}. Therefore, eco-friendly practices for reusing agricultural waste are 
essential to mitigate both environmental and health hazards.

Moreover, inefficiencies in supply chain networks for food and agricultural products, including pistachios, can have significant 
implications for product quality and trade \citep{casper2018}. Addressing these environmental and logistical concerns is crucial to sustaining both 
the economic and ecological viability of the pistachio industry.  In the broader context of sustainable supply chains, several studies have focused on 
optimizing resource use and minimizing waste through recovery and recycling practices. For example, sustainable supply chain models designed for 
agriculture and other industries have shown significant environmental benefits by reducing waste and reusing by-products for processes like 
biogas and compost production \citep{cheraghalipour2022}. Such approaches have been successfully applied in sectors such as manufacturing 
and waste recovery, where efficient resource management and cost reduction have been key drivers \citep{devika2014, zohal2016}. 
These strategies underline the importance of sustainability in modern supply chains and the potential for agricultural industries to implement similar measures.
% Reviewer~4, comment~8: explicit scoping of the sustainability claim. The model routes by-products to composting
% centres but optimises cost alone; it quantifies no emissions, landfill avoidance or resource-recovery indicator.
It should be stated at the outset, however, that sustainability enters the model studied here only structurally, through the routing of by-products to composting
centres, and not as an optimisation criterion. The objective function minimises facility, production and transportation costs alone; no environmental or social
indicator is quantified or optimised. Extending the formulation with explicit sustainability objectives is a natural continuation of this work, but it is outside
the scope of the present study, whose contribution is algorithmic.

In related research, \cite{rajabi2023} focused on optimizing the pistachio supply chain using various algorithms, primarily population-based 
approaches, such as the Genetic Algorithm (GA) \citep{fathollahi2018, xie2022}, along with several metaphor-based metaheuristics including the Imperialist Competitive 
Algorithm and Social Engineering Optimizer. However, one aspect that could be improved in their methodology is the level of detail provided for reproducibility. 
Specifically, the authors employed a real-valued encoding scheme without detailing the decoding procedure required to transform these values into a feasible transport tree. 
This omission hinders reproducibility and obscures the mechanism by which constraints are handled. Additionally, recent critical analyses \citep{campelo2023, aranha2022}
have demonstrated that many metaphor-based metaheuristics offer limited algorithmic innovation beyond terminological variations, often performing comparably to (or worse than) classical, 
well-established methods. Given these findings, this work adopts a more transparent approach by employing the Variable Neighborhood Search (VNS), a well-understood classical metaheuristic 
with clear operational principles, extensive theoretical foundation, and proven effectiveness across diverse optimization problems.

Addressing these gaps, this research focuses on designing an optimal supply chain network for pistachios by leveraging the Variable Neighborhood Search (VNS) algorithm \citep{gendreau2010}. 
While we adopt the same mixed-integer linear programming (MILP) model formulation from \cite{rajabi2023}, we propose a robust overhaul of the resolution methodology. Adopting the same
model is deliberate: it makes the comparison against prior work direct, and isolates the resolution methodology as the object of study.

% Reviewer~1 comment~1 and Reviewer~2 comment~3: the encoding is positioned as an adopted design choice, not as a
% contribution. The contributions are reordered so that the decoding specification and the operators come first.
The contributions of this work are therefore threefold. First, we provide a complete, step-by-step specification of the decoding algorithm that maps a priority chromosome onto a
feasible transportation tree for this network, including the reconciliation of the jointly produced outputs at processing centres. The representation itself is not new: priority-based
encodings are well established in network design \citep{gen2006, altiparmak2006}, and we adopt one rather than propose one. What prior work on this problem leaves unspecified is the
decoding procedure, without which the results cannot be reproduced and the constraint-handling mechanism cannot be inspected. Second, we develop four problem-specific neighborhood
operators that exploit the distinction, induced by the decoder, between facilities that carry flow and those that do not; these are contrasted with classical permutation moves to
demonstrate their efficacy in exploring the solution space. Third, we report a systematic benchmark against a Genetic Algorithm, an Iterated Local Search and an exact solver across
seven instance scales, under a computational budget and a set of shared components that make the comparison directly interpretable.

To validate the proposed approach, we conduct a comprehensive comparative analysis against a Genetic Algorithm (GA), an Iterated Local Search (ILS), and an exact Branch-and-Cut method 
(using the Gurobi solver). Our findings demonstrate that the proposed VNS with specialized operators consistently outperforms the other metaheuristics in terms of solution quality. 
On top of that, we show that for large-scale instances, where exact methods become infeasible due to memory limitations and excessive runtime, the proposed heuristic offers a vital trade-off, 
providing high-quality feasible solutions efficiently.

The remainder of this paper is organized as follows. Section~\ref{sec:related-work} positions the present study within the literature on agri-food network design,
priority-based encodings, and structure-aware neighborhoods. Section~\ref{sec:problem-definition} presents the problem definition and the mathematical formulation. Section~\ref{sec:methodology}
details the heuristic approach, introducing the integer encoding, the specific decoding steps, and the domain-specific neighborhood structures. Section~\ref{sec:experiments}
reports the computational experiments, offering a statistical comparison between VNS, GA, ILS, and the exact solver across varying instance sizes. Finally, Section~\ref{sec:conclusion}
summarizes the key findings and suggests future research directions.

% ----------------------------------------------------------------------------------------------------------------------
% =====================================================================================================================
% TODO(revision): Section to be written. Addresses Reviewer #2 comment 2 (positioning in the literature) and provides
% the evidence base for the responses to Reviewer #2 comment 1 and Reviewer #4 comment 1 (limited methodological
% novelty). Each subsection below states the argument it must establish.
% =====================================================================================================================
\section{Related Work}
\label{sec:related-work}

\textbf{[TO BE WRITTEN]} \textit{Preamble stating the three bodies of literature this work draws on and the specific gap it addresses.}

% ----------------------------------------------------------
\subsection{Agri-food Supply Chain Network Design}
\label{subsec:rw_agrifood}

\textbf{[TO BE WRITTEN]} \textit{Must establish what distinguishes the pistachio setting from generic multi-echelon network design. The key structural
differentiator is the joint production at processing centers: a single input stream splits into open-shell pistachios and raw kernels in fixed conversion
proportions, so satisfying one downstream demand mechanically creates surplus of the other output. This is what the reconciliation step of the decoder
(Algorithms \ref{algoDecode1}--\ref{algoDecode2}) exists to handle, and it is currently underexploited as a contribution. Also covers by-product valorization
and the aflatoxin/waste-handling motivation.}

% ----------------------------------------------------------
\subsection{Priority-Based Encodings for Network Design}
\label{subsec:rw_encoding}

\textbf{[TO BE WRITTEN]} \textit{Must trace the lineage of the representation and state plainly that it is adopted, not invented: Gen and Cheng, \citet{gen2006},
random-key representations, and their use in multi-echelon logistics. Must then justify the specific design choice made here (integer permutation over random
keys) on genotype-to-phenotype grounds, and note that the encoding is linear in the number of facilities, since one priority is assigned per node rather than
per arc. Addresses Reviewer~1 comment 1 and Reviewer~2 comment 3.}

% ----------------------------------------------------------
\subsection{Structure-Aware Neighborhoods in Metaheuristics}
\label{subsec:rw_neighborhoods}

\textbf{[TO BE WRITTEN]} \textit{Must survey phenotype-aware, solution-dependent, and facility-activation neighborhoods, and establish explicitly how the four
operators proposed here differ from that prior art. Reviewer~4 comment 1 states that this distinction is not adequately established, so the subsection must
end with a direct comparison rather than a list.}

% ----------------------------------------------------------
\subsection{Variable Neighborhood Search and Hybrid Variants}
\label{subsec:rw_vns}

\textbf{[TO BE WRITTEN]} \textit{Must acknowledge that VNS/VND combinations with probabilistic acceptance are established, and reposition the contribution
accordingly: the claim is not a new hybrid template but the neighborhood structures and their empirical validation. Addresses Reviewer~2 comment 1 and
Reviewer~4 comment 1.}

% ----------------------------------------------------------------------------------------------------------------------
\section{Problem Definition}
\label{sec:problem-definition}

The logistics network proposed by \cite{rajabi2023}, illustrated in Figure~\ref{fig:supplychain}, describes the flow of products and by-products along the pistachio supply chain. 
The process begins with the farmers ($i$), who send the harvested pistachios to the processing centers ($j$). At these centers, the pistachios are transformed into three main components: 
open-shell pistachios, raw kernels, and organic waste.

Each of these components follows specific flows. Open-shell pistachios are forwarded to pistachio factories ($k$), which process and distribute them to final customers ($n_1$). 
Raw kernels are sent to oil extraction centers ($e$), responsible for producing pistachio oil, which is directed to both final customers ($n_2$) and cosmetic factories ($s$). 
These, in turn, process the oil into cosmetic products that are supplied to their respective consumers ($n_3$). Organic waste generated in the initial processing is sent to 
composting centers ($q$), where it is transformed into compost, subsequently distributed to composting customers ($m$).

Although in practical terms, the produced compost could be reused by the original farmers, establishing a truly closed-loop system, the adopted mathematical model does not explicitly 
represent this feedback. Compost customers ($m$) are modeled as distinct entities from the initial farmers ($i$), thus the feedback cycle is not enforced through model constraints. This simplification, already 
adopted in previous works \citep{pishvaee2010,ahmadi2019,banasik2016}, preserves the structural simplicity of the model and computational tractability. However, explicitly 
modeling this return loop with mass balance constraints represents a promising direction for future investigations, especially in the context of sustainable supply chains.

\begin{figure}
  \begin{center}
      \includegraphics[width=10cm]{./supplychain.png}
      \caption{Structure of the proposed pistachio supply chain.}
      \label{fig:supplychain}
  \end{center}
\end{figure}

The mixed linear problem aims to minimize total costs \eqref{eq:custo}, including opening, production, and transportation costs, subject to the following capacity and demand constraints, 
tolerating no shortages. Key model parameters include: $\beta$ (weight loss percentage during drying operations at processing centers); $\theta_1$, $\theta_2$, and $\theta_3$ 
(waste percentages of open-shell pistachios, raw kernels, and organic waste, respectively, where $\theta_1 + \theta_2 + \theta_3 = 1$); $\gamma_k$, $\gamma_s$, and $\gamma_q$ 
(production rates for pistachio factories, cosmetic factories, and composting centers, respectively); and $\lambda$ (waste percentage during oil extraction). 
A complete description of all parameters and decision variables is provided in Table 5 of \cite{rajabi2023}.

\begin{align}
    \min &\ z_1 + z_2 + z_3 \label{eq:custo}\\
    \text{s.t.:} \quad & \sum\limits_{j=1}^J X_{ij} \le Cpa_i, \quad \forall i \in I \label{eq:restricao1}\\
    & \sum\limits_{i=1}^I X_{ij} \le Cpu_j\times U_j, \quad \forall j \in J \label{eq:restricao2}\\
    & \sum\limits_{e=1}^E Ow_{eq} + \sum\limits_{j=1}^J Gw_{jq} \le Cpy_q \times Y_q, \quad \forall q \in Q \label{eq:restricao3} \\
    & \sum\limits_{j=1}^J Go_{jk} \le Cpw_k\times W_k, \quad \forall k \in K \label{eq:restricao4}\\
    & \sum\limits_{j=1}^J Gr_{je} \le Cpr_e\times R_e, \quad \forall e \in E \label{eq:restricao5}\\
    & \sum\limits_{e=1}^E O_{en_2} \le Cpv_s\times V_s, \quad \forall s \in S \label{eq:restricao6}\\
    & \sum\limits_{k=1}^K Go_{jk} + \sum\limits_{e=1}^E Gr_{je} + \sum\limits_{q=1}^Q Gw_{jq} \le \sum\limits_{i=1}^I X_{ij}, \quad \forall j \in J \label{eq:restricao7} \\
    & \sum\limits_{k=1}^K Go_{jk} = (1-\beta) \sum\limits_{i=1}^I X_{ij}\theta_1, \quad \forall j\in J \label{eq:restricao8} \\
    & \sum\limits_{e=1}^E Gr_{je} = (1-\beta) \sum\limits_{i=1}^I X_{ij}\theta_2, \quad \forall j\in J \label{eq:restricao9} \\
    & \sum\limits_{q=1}^Q Gw_{jq} = \sum\limits_{i=1}^I X_{ij}\theta_3, \quad \forall j\in J \label{eq:restricao10} \\
    & \theta_1 + \theta_2 + \theta_3 = 1 \label{eq:restricao11} \\
    & \sum\limits_{n_1=1}^{N_1} P_{kn_1} \le \sum\limits_{j=1}^J Go_{jk} \times \gamma_k, \quad \forall k \in K \label{eq:restricao12}
\end{align}

\begin{align}
        & \sum\limits_{n_2=1}^{N_2} O_{en_2} + \sum\limits_{s=1}^S Oc_{es} \le \sum\limits_{j=1}^J Gr_{je}\times(1-\lambda), \quad \forall e\in E \label{eq:restricao13} \\
    & \sum\limits_{q=1}^{Q} Ow_{eq} \le \sum\limits_{j=1}^J Gr_{je} \times \lambda, \quad \forall e \in E \label{eq:restricao14} \\
    & \sum\limits_{n_3=1}^{N_3} L_{sn_1} \le \sum\limits_{e=1}^E Oc_{es} \times \gamma_s, \quad \forall s \in S \label{eq:restricao15} \\
    & \sum\limits_{m=1}^M D_{qm} \le \left(\sum\limits_{j=1}^J Gw_{jq} + \sum\limits_{e=1}^E Ow_{eq} \right) \times \gamma_q, \quad \forall q \in Q \label{eq:restricao16} \\
    & \sum\limits_{k=1}^K P_{kn_1} \ge Dp_{n_1}, \quad \forall n_1 \in N_1 \label{eq:restricao17}\\
    & \sum\limits_{e=1}^E O_{en_2} \ge Du_{n_2}, \quad \forall n_2 \in N_2 \label{eq:restricao18}\\
    & \sum\limits_{s=1}^S L_{sn_3} \ge Ds_{n_3}, \quad \forall n_3 \in N_3 \label{eq:restricao19}\\
    & \sum\limits_{q=1}^Q D_{qm} \le Dc_{m}, \quad \forall m \in M \label{eq:restricao20}
\end{align}
    
\begin{itemize}
\item The objective function \eqref{eq:custo} minimizes the total costs of the supply chain, including opening, transportation, and production costs. The costs are divided into three components: 
$z_1$ \eqref{eq:custo1}, $z_2$ \eqref{eq:custo2}, and $z_3$ \eqref{eq:custo3}, which represent facility opening costs, transportation costs, and production costs, respectively, as shown in the following equations:
\end{itemize}

\begin{align}
  z_1 &= \sum_{j=1}^J Fu_j \times U_j 
      + \sum_{q=1}^Q Fy_q \times Y_q 
      + \sum_{k=1}^{K} Fw_k \times W_k \notag \\
      &\quad + \sum_{e=1}^{E} Fr_e \times R_e
      + \sum_{s=1}^S Fv_s \times V_s
      \label{eq:custo1}
\end{align}

\begin{align}
  z_2 &= \sum_{i=1}^I\sum_{j=1}^J CI_i \times X_{ij} 
      + \sum_{j=1}^J\sum_{k=1}^K Cu_{1j} \times Go_{jk} \notag \\
      &\quad + \sum_{j=1}^J\sum_{e=1}^E Cu_{2j} \times Gr_{je} 
      + \sum_{q=1}^Q\sum_{m=1}^M Cy_q \times D_{qm} \notag \\
      &\quad + \sum_{k=1}^K\sum_{n_1=1}^{N_1} Cw_k \times P_{kn_1} \notag \\
      &\quad + \sum_{e=1}^E Cr_e \times \left( \sum_{n_2=1}^{N_2} O_{en_2} 
      + \sum_{s=1}^S Oc_{es} \right) \notag \\
      &\quad + \sum_{s=1}^S\sum_{n_3=1}^{N_3} Cv_s \times L_{sn_3}
      \label{eq:custo2}
\end{align}

\begin{align}
  z_3 &= \sum_{i=1}^I\sum_{j=1}^J CX_{ij} \times X_{ij} 
      + \sum_{j=1}^J\sum_{k=1}^K CK_{jk} \times Go_{jk} \notag \\
      &\quad + \sum_{j=1}^J\sum_{q=1}^Q CJ_{jq} \times Gw_{jq}
      + \sum_{e=1}^E\sum_{s=1}^S CS_{es} \times Oc_{es} \notag \\
      &\quad + \sum_{e=1}^E\sum_{n_2=1}^{N_2} CN_{en_2} \times Oc_{en_2}
      + \sum_{e=1}^E\sum_{q=1}^Q CQ_{eq} \times Ow_{eq} \notag \\
      &\quad + \sum_{s=1}^S\sum_{n_3=1}^{N_3} Cl_{sn_3} \times L_{sn_3}
      + \sum_{k=1}^K\sum_{n_1=1}^{N_1} Cp_{kn_1} \times P_{kn_1} \notag \\
      &\quad + \sum_{q=1}^Q\sum_{m=1}^M Cd_{qm} \times D_{qm}
      \label{eq:custo3}
\end{align}

\begin{itemize}
  \item The variables $U_j$, $Y_q$, $W_k$, $R_e$, and $V_s$ are binary and determine whether certain elements of the model are active. For example, $U_j = 1$ indicates that processing center $j$ is 
  operational, and $U_j = 0$ otherwise. Similarly, $W_k$, $R_e$, $V_s$, and $Y_q$ indicate whether pistachio factories, oil extraction centers, cosmetic factories, and composting centers are active, 
  respectively. The other variables in the model are continuous and non-negative, representing real values greater than or equal to zero. These variables model flows, 
  quantities, and other measurable factors within the system.
  
  \item Constraints \eqref{eq:restricao1}-\eqref{eq:restricao6} address the capacity and demand limitations of each facility in the supply chain. In particular, constraint \eqref{eq:restricao1} 
  limits the quantity of products transported from farmers to distribution centers. Constraints \eqref{eq:restricao2}-\eqref{eq:restricao6} ensure that the quantities sent to processing centers, 
  composting centers, pistachio factories, oil extraction centers, and cosmetic factories do not exceed the capacities of these facilities.
  
  \item The quantity of products shipped from any facility must not exceed the orders received by that facility. Consequently, constraint \eqref{eq:restricao7} establishes that the quantity of 
  products shipped from a processing center must be less than or equal to the quantity received by other facilities. Constraints \eqref{eq:restricao8}-\eqref{eq:restricao11} define the 
  quantities of open-shell pistachios, raw kernels, and waste generated during the hulling process at each processing center. Similarly, constraints \eqref{eq:restricao12}-\eqref{eq:restricao16} 
  govern the flows within pistachio factories, oil extraction centers, cosmetic factories, and composting centers.
  
  \item Finally, to ensure that customer demand is met, constraints \eqref{eq:restricao17}-\eqref{eq:restricao20} ensure that the quantities of products delivered to pistachio, pistachio oil, 
  cosmetics, and compost customers satisfy or exceed their respective demands.
\end{itemize}

Having defined the mathematical formulation of the pistachio supply chain optimization problem, we now present our solution methodology based on Variable Neighborhood Search with 
problem-specific operators designed to exploit the structure of this complex multi-echelon network.

% ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
\section{Methodology}
\label{sec:methodology}

This section details the proposed Variable Neighborhood Search (VNS) algorithm with priority-based encoding. Section~\ref{subsec:solution_rep} presents the integer-based
chromosome representation and Section~\ref{subsec:decoding} the backward decoding strategy that maps it onto a feasible transportation tree. Section~\ref{subsec:neighborhoods}
then defines the problem-specific neighborhood structures designed to exploit the pistachio supply chain's structural properties, and Section~\ref{subsec:vns} describes how the
search procedure combines them.

% ----------------------------------------------------------
\subsection{Solution Representation}
\label{subsec:solution_rep}

Representing a solution for a complex network design problem is non-trivial. Following the principles established by \citet{gen2006} and \cite{rajabi2023},
we employ a priority-based encoding method. In this scheme, the chromosome does not directly encode the flow quantities ($X_{ij}$), but rather the \textit{priority}
with which each node is considered when flows are assigned.

% Reviewer~1, comment~1 and Reviewer~2, comment~3: explicit statement that the representation is adopted rather
% than proposed, so that the contribution is not misread as the encoding itself.
It should be stated plainly that this representation is not a contribution of the present work. Priority-based encodings are well established in network design and have been applied
extensively to transportation and multi-echelon logistics problems \citep{gen2006, altiparmak2006}. What we contribute is not the representation but, first, the complete specification
of the decoding procedure that turns it into a feasible solution for this particular network (Section~\ref{subsec:decoding}) and, second, the neighborhood structures that exploit the
structure the decoder induces (Section~\ref{subsec:neighborhoods}). The two design decisions we do make explicit here, and justify below, are the use of an integer permutation in place
of a real-valued random key, and the partition of the chromosome into one segment per transportation stage.

\subsubsection{Integer Permutation vs. Real-Valued Encoding}
In this work, we use an integer permutation encoding rather than the random key ($0-1$ real value) encoding used in previous studies \citep{rajabi2023, fathollahi2018}.

In the real-valued approach, genes are assigned random numbers $r \in [0, 1]$, and priorities are derived from sorting these values. This representation suffers from a "many-to-one" 
mapping redundancy: infinite combinations of real numbers can result in the exact same priority order. For example, the vectors $v_1 = [0.1, 0.2, 0.5]$ and $v_2 = [0.8, 0.85, 0.9]$ 
both map to the same permutation indices $(1, 2, 3)$. This redundancy creates "flat" regions in the search landscape where small perturbations (mutations) in the genotype produce no 
change in the phenotype (the solution structure).

In contrast, we employ sequential integers from $1$ to $Z$, where $Z$ is the number of \emph{nodes} involved in a given chain segment, that is, the number of sources plus the number
of destinations of that transportation stage. This ordinal representation ensures a one-to-one mapping between the genotype and the priority ordering. This property is crucial for the
VNS algorithm, as it ensures that every neighborhood move yields a distinct priority ordering, eliminating the redundant search steps that a random-key encoding admits.

% ----------------------------------------------------------
\subsubsection{Chromosome Structure}
\label{subsubsec:chromosome}
The solution is represented by a composite chromosome divided into eight distinct segments ($S_1$ to $S_8$), as illustrated in Fig. \ref{fig:cromossomo}. Each segment contains a
permutation of integers representing the transport priorities for a specific part of the supply chain.

% =====================================================================================================================
% Reviewer~4, comment~3 states that the chromosome length should be quadratic in N. It is not, and the source of the
% misreading was the previous wording, which described the segments as encoding source-destination *pairs*. In a
% priority-based encoding one priority is assigned per node, not per arc, so a segment covering a stage with |A|
% sources and |B| destinations has length |A| + |B|, and the total is linear in the number of facilities. The
% paragraph below states this explicitly and presents the linearity as the scalability property it is.
% =====================================================================================================================
Each segment corresponds to one transportation stage and assigns one priority value \emph{per node} of that stage, rather than one value per arc. A segment covering a stage with a set
of sources $\mathcal{A}$ and a set of destinations $\mathcal{B}$ therefore has length $|\mathcal{A}| + |\mathcal{B}|$, and holds a permutation of $1, \dots, |\mathcal{A}| + |\mathcal{B}|$.
The arcs themselves are never enumerated in the genotype: which arcs carry flow, and how much, is determined by the decoder of Section~\ref{subsec:decoding}, which repeatedly selects the
highest-priority node still available and assigns to it the largest admissible flow. Writing $L$ for the total chromosome length over the eight stages,
%
\begin{equation}
\label{eq:chromo_length}
L = \underbrace{(K + N_1)}_{S_1} + \underbrace{(S + N_3)}_{S_2} + \underbrace{(E + N_2 + S)}_{S_3} + \underbrace{(J + K)}_{S_4} + \underbrace{(J + E)}_{S_5} + \underbrace{(I + J)}_{S_6} + \underbrace{(Q + M)}_{S_7} + \underbrace{(E + Q)}_{S_8},
\end{equation}
%
so that $L$ grows linearly with the number of facilities per echelon. This is a deliberate and consequential property of the representation: an arc-indexed encoding of the same network
would grow quadratically, which is precisely what makes such encodings impractical at the scales considered in Section~\ref{sec:experiments}. The cost of this compactness is that the
genotype does not determine the solution on its own, which is why the decoding procedure must be specified in full rather than left implicit.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{./chromo.PNG}
    \caption{Structure of the priority-based chromosome. The solution is composed of 8 distinct integer permutations ($S_1-S_8$), each governing the transport priority of a specific subsection in the supply chain.}
    \label{fig:cromossomo}
\end{figure}

% ----------------------------------------------------------
\subsection{Decoding Procedure}
\label{subsec:decoding}

The decoding process serves as a constructive heuristic that transforms the priority chromosome into a feasible transportation tree. As shown in Fig. \ref{fig:decode_flux}, the decoding 
process begins with the flow of final products (pistachios, oil, cosmetics) from factories to consumers, working backward through the supply chain. This backward approach is preferred 
over forward decoding because it naturally respects demand constraints: consumer demand is satisfied first, propagating the requirements upstream to factories and processing centers. 
This strategy guarantees feasibility by ensuring that facility capacities are respected at every stage while preventing overproduction, a critical consideration in perishable 
agricultural supply chains.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.9\textwidth]{./decode_flux.PNG}
  \caption{The backward decoding flow. The process prioritizes satisfying end-consumer demands first to propagate requirements upstream, ensuring feasibility throughout the chain.}
  \label{fig:decode_flux}
\end{figure}

As illustrated in Figure \ref{fig:decode_flux}, the decoding process initiates with the flow of derived pistachio products from factories to consumers, ensuring all 
constraints are satisfied. Oil and cosmetic product flows follow similar principles. Processing centers regulate pistachio allocation, accounting for production losses 
and ensuring viable distribution. Harvested pistachios are allocated proportionally between factories and oil extraction centers, prioritizing demand satisfaction and 
minimizing transportation costs. Flows between producers and processing centers cannot exceed production capacity, guaranteeing system feasibility. Waste 
flows are managed similarly, respecting processing and composting capacity limits while minimizing costs.

To decode each logistic flow in the supply chain, we employ the \texttt{decodingStep} function, which implements the priority-based transportation algorithm described 
by \citet{gen2006}. This function acts iteratively, allocating product units from source to destination based on a chromosome vector that defines the priority order 
among logistic nodes, always respecting source capacities, destination demands, and unit transportation costs. The \texttt{decodingStep} function corresponds directly 
to the priority-based procedure detailed in Table 2 of \citet{gen2006}.

Algorithms \ref{algoDecode1} and \ref{algoDecode2} detail the complete decoding process in two phases (separated due to page constraints). Algorithm \ref{algoDecode1} 
initiates the process by decoding the main flows between factories, extraction centers, consumers, and processing centers using chromosome segments $S_1$ through $S_5$. 
The critical logic occurs in Steps 5 and 6, where the algorithm reconciles diverging flows from processing centers, which produce multiple outputs (Kernels for Oil vs. 
Open-shell for Factories) in fixed proportions. Satisfying one downstream demand often creates surplus of the other output. The decoding identifies the bottleneck demand 
and routes surplus to the lowest-cost destination, minimizing over-supply penalties. Critically, this reconciliation mechanism explicitly enforces the joint production 
equality constraints \eqref{eq:restricao8} and \eqref{eq:restricao9}. After initial flows are allocated, the algorithm computes the implied input requirement from each 
processing center under both scenarios (if $G_o$ dictates capacity vs. if $G_r$ dictates capacity), selects the binding constraint, and augments the non-binding output 
flow to maintain the exact proportions $(1-\beta)\theta_1$ and $(1-\beta)\theta_2$. This adjustment step transforms inequality-based greedy allocation into an equality-satisfying 
solution, ensuring that the mass balance at processing centers rigorously conforms to the fixed conversion ratios defined by the waste percentages.

Algorithm \ref{algoDecode2} performs additional adjustments to ensure consistency between inputs and outputs at intermediate centers, decodes transport from producers 
to processing centers (segment $S_6$), allocates waste generated in the processes to composting centers (segments $S_7$ and $S_8$), and determines which facilities 
should be opened based on the obtained flows. At the end of the process, a complete set of continuous and binary decision variables is obtained, representing a feasible 
and coherent solution to the proposed optimization problem.

\begin{algorithm}
  \caption{Chromosome decoding for the supply chain (Part 1)}
  \label{algoDecode1}
  \begin{algorithmic}[1]
  \Procedure{Decode}{data}
  \State \textbf{Input:} structured data containing:
  \State - Capacities ($\mathbf{C}$) and demands ($\mathbf{D}$) for all facilities
  \State - Unit transportation costs ($\mathbf{T}$)
  \State - Loss rates for each process stage
  \State - Consumer demands
  \State - Chromosome segments ($S_1, S_2, \ldots, S_8$) representing transport priorities
  \State \textbf{Output:} TotalCost, Decision Variables ($\mathbf{X}, \mathbf{P}, \mathbf{L}, \mathbf{O}, \dots$)
  
  \State \textbf{// Step 1: Initialize variables}
  \State $TotalCost \leftarrow 0$

  \State \textbf{// Step 2: Decode Pistachio Factories $\rightarrow$ Pistachio Consumers}
  \State $a_1 \leftarrow$ capacities of pistachio factories
  \State $b_1 \leftarrow$ demands of pistachio consumers
  \State $c_1 \leftarrow$ transportation costs
  \State $(cost1, \mathbf{P}) \leftarrow \text{decodingStep}(S_1, a_1, b_1, c_1)$
  \State $TotalCost \leftarrow TotalCost + cost1$
  
  \State \textbf{// Step 3: Decode Cosmetic Factories $\rightarrow$ Cosmetic Consumers}
  \State $a_2 \leftarrow$ capacities of cosmetic factories
  \State $b_2 \leftarrow$ demands of cosmetic consumers
  \State $c_2 \leftarrow$ transportation costs
  \State $(cost2, \mathbf{L}) \leftarrow \text{decodingStep}(S_2, a_2, b_2, c_2)$
  \State $TotalCost \leftarrow TotalCost + cost2$
  
  \State \textbf{// Step 4: Decode Oil Extraction Centers $\rightarrow$ Consumers}
  \State $a_3 \leftarrow$ capacities of oil extraction centers
  \State $b_3 \leftarrow$ demands of oil consumers + cosmetic factories
  \State $c_3 \leftarrow$ transportation costs
  \State $(cost3, OO_c) \leftarrow \text{decodingStep}(S_3, a_3, b_3, c_3)$
  \State Separate $OO_c$ into $\mathbf{O}$ (oil consumers) and $O_c$ (cosmetic factories)
  \State $TotalCost \leftarrow TotalCost + cost3$
  
  \State \textbf{// Step 5: Decode Processing Center $\rightarrow$ Pistachio Factories}
  \State $a_4 \leftarrow$ capacities of processing centers
  \State $b_4 \leftarrow$ adjusted demands of pistachio factories
  \State $c_4 \leftarrow$ transportation costs
  \State $(\_\_, G_o) \leftarrow \text{decodingStep}(S_4, a_4, b_4, c_4)$
  
  \State \textbf{// Step 6: Decode Processing Center $\rightarrow$ Oil Extraction Centers}
  \State $a_5 \leftarrow$ capacities of processing centers
  \State $b_5 \leftarrow$ adjusted demands of oil extraction centers
  \State $c_5 \leftarrow$ transportation costs
  \State $(\_\_, G_r) \leftarrow \text{decodingStep}(S_5, a_5, b_5, c_5)$
  \EndProcedure
  \end{algorithmic}
\end{algorithm}

\begin{algorithm}
  \caption{Chromosome decoding (Part 2): Upstream flows and waste management}
  \label{algoDecode2}
  \begin{algorithmic}[1]
  \Procedure{Decode}{continuation from Algorithm \ref{algoDecode1}}
  \State \textbf{// Step 7: Adjust demands at Processing Centers}
  \For{each processing center $j$}
      \State Calculate required outputs based on pistachio and oil flows
      \State Update $G_o$ and $G_r$ to match effective capacities
  \EndFor

  \State \textbf{// Step 8: Decode Pistachio Farmers $\rightarrow$ Processing Centers}
  \State $a_6 \leftarrow$ capacities of pistachio farmers
  \State $b_6 \leftarrow$ updated demands at processing centers
  \State $c_6 \leftarrow$ transportation costs
  \State $(cost6, \mathbf{X}) \leftarrow \text{decodingStep}(S_6, a_6, b_6, c_6)$
  \State $TotalCost \leftarrow TotalCost + cost6$
    
  \State \textbf{// Step 9: Decode Composting Centers $\rightarrow$ Compost Consumers}
  \State $a_7 \leftarrow$ capacities of composting centers
  \State $b_7 \leftarrow$ demands of compost consumers
  \State $c_7 \leftarrow$ transportation costs
  \State $(cost7, \mathbf{D}) \leftarrow \text{decodingStep}(S_7, a_7, b_7, c_7)$
  \State $TotalCost \leftarrow TotalCost + cost7$
    
  \State \textbf{// Step 10: Assign waste flows}
  \State Calculate waste from processing centers ($J$) and Oil Centers ($E$)
  \State $a_8 \leftarrow$ available capacities of composting centers
  \State $b_8 \leftarrow$ waste demands to be disposed
  \State $c_8 \leftarrow$ transportation costs
  \State $(\_\_, O_w) \leftarrow \text{decodingStep}(S_8, a_8, b_8, c_8)$
  \State Update $TotalCost$
    
  \State \textbf{// Step 11: Define binary opening variables}
  \State Set $\mathbf{U}, \mathbf{W}, \mathbf{R}, \mathbf{V}, \mathbf{Y} = 1$ if corresponding flow $> 0$
  \State Add fixed costs to $TotalCost$
    
  \State \textbf{return} $TotalCost$, Decision Variables, Binary Variables
  \EndProcedure
  \end{algorithmic}
\end{algorithm}

% ----------------------------------------------------------
\subsection{Neighborhood Structures}
\label{subsec:neighborhoods}

A critical inefficiency in applying standard permutation operators to priority-based encoding is their "blindness" to the phenotypic state.
In the greedy decoding process (Algorithm \ref{algoDecode1}), typically only a subset of high-priority nodes is required to satisfy all demands.
We term these Active Nodes ($\mathcal{A}$), while the remaining unused nodes are Inactive ($\mathcal{I}$). Standard operators
often swap two Inactive nodes, changing the genotype but resulting in the exact same transportation tree and objective value.

To overcome this redundancy, our implementation explicitly tracks the set of active nodes returned by the decoder and employs four structure-aware
operators. These operators are designed to either intensify the search within the current topology or diversify it by forcing interactions between
the active and inactive sets.

% ----------------------------------------------------------
\subsubsection{Notation}
\label{subsubsec:op_notation}

% Reviewer~2, comment~4 and Reviewer~4, comment~1 both ask for formal definitions rather than conceptual descriptions.
% The definitions below follow the implementation exactly.
Let $s \in \{1, \dots, 8\}$ index the transportation stages of Section~\ref{subsubsec:chromosome} and let $V_s$ denote the set of nodes of stage $s$, with $Z_s = |V_s|$. A chromosome
is the tuple $\pi = (\pi_1, \dots, \pi_8)$, where $\pi_s : V_s \rightarrow \{1, \dots, Z_s\}$ is a bijection assigning a distinct priority to each node, and higher values denote higher
priority. Nodes of $V_s$ are held in a fixed order, and we write $\mathrm{pos}(v) \in \{1, \dots, Z_s\}$ for the position of node $v$ in that order; note that $\mathrm{pos}(v)$ is a
property of the instance, whereas $\pi_s(v)$ is a property of the solution.

Decoding $\pi$ by the procedure of Section~\ref{subsec:decoding} yields, besides the flows and the objective value, an activation indicator $\alpha_s : V_s \rightarrow \{0,1\}$, where
$\alpha_s(v) = 1$ if node $v$ carries strictly positive flow in the decoded solution. This induces the partition
%
\begin{equation}
\label{eq:active_partition}
\mathcal{A}_s = \{\, v \in V_s : \alpha_s(v) = 1 \,\}, \qquad \mathcal{I}_s = V_s \setminus \mathcal{A}_s ,
\end{equation}
%
which is what distinguishes these operators from classical permutation moves: $\mathcal{A}_s$ is not available from the genotype and must be read back from the decoder. Each operator
below first draws a stage $s$ uniformly at random from $\{1, \dots, 8\}$ and then modifies $\pi_s$ alone, leaving the remaining seven segments untouched. Every operator is a no-op
whenever its precondition on $\mathcal{A}_s$ or $\mathcal{I}_s$ fails, in which case another stage is drawn. We write $\pi'$ for the resulting chromosome and omit the subscript $s$
where no ambiguity arises.

% ----------------------------------------------------------
\subsubsection{Operator Definitions}
\label{subsubsec:op_definitions}

\paragraph{Active Reversion.} \textit{Precondition:} $|\mathcal{A}| \ge 2$. Let $v_{(1)}, \dots, v_{(|\mathcal{A}|)}$ be the active nodes ordered by position, and draw two distinct
ranks $r_1 < r_2$ uniformly at random. The operator reverses the priorities carried by the active nodes ranked between $r_1$ and $r_2$,
%
\begin{equation}
\label{eq:op_activereversion}
\pi'\!\left(v_{(r_1 + k)}\right) = \pi\!\left(v_{(r_2 - k)}\right), \quad k = 0, \dots, r_2 - r_1 ,
\end{equation}
%
and leaves $\pi'(u) = \pi(u)$ for every other node. Note that the reversal is applied to the \emph{subsequence} of active nodes, not to a contiguous block of $V$: inactive nodes lying
between $v_{(r_1)}$ and $v_{(r_2)}$ retain their priorities. Since $\mathcal{A}$ is preserved as a set and only the order within it changes, this is the intensification move of the
four: it redistributes flow among facilities already in use, without opening or closing any facility, so it explores lower-cost allocations within the current network topology.

\paragraph{Inactive-Active Swap.} \textit{Precondition:} $\mathcal{A} \neq \emptyset$ and $\mathcal{I} \neq \emptyset$. Draw $u \in \mathcal{A}$ and $v \in \mathcal{I}$ independently
and uniformly, and exchange their priorities,
%
\begin{equation}
\label{eq:op_iaswap}
\pi'(u) = \pi(v), \qquad \pi'(v) = \pi(u), \qquad \pi'(w) = \pi(w) \;\; \forall\, w \notin \{u, v\} .
\end{equation}
%
Because $\pi(u) > \pi(v)$ need not hold, the move can promote or demote either node; what it guarantees is that a currently used facility and a currently unused one exchange their
standing in the priority order, which is the global diversification mechanism of the set.

\paragraph{Inactive Boost.} \textit{Precondition:} $\mathcal{I} \neq \emptyset$. Draw $v \in \mathcal{I}$ uniformly and promote it above every other node, compressing the priorities it
overtakes so that their relative order is preserved,
%
\begin{equation}
\label{eq:op_boost}
\pi'(w) =
\begin{cases}
\pi(w) - 1, & \text{if } \pi(w) > \pi(v), \\[2pt]
\pi(w), & \text{if } \pi(w) < \pi(v), \\[2pt]
\displaystyle \max_{u \in V} \pi(u) + 1, & \text{if } w = v .
\end{cases}
\end{equation}
%
This is the most aggressive of the four moves. It addresses the starvation effect characteristic of capacity-constrained problems: a low-cost facility may never enter a solution simply
because it sits below enough others that demand is exhausted before it is reached, and no pairwise exchange of adjacent priorities is enough to rescue it. Forcing it to the top makes
the decoder route the maximum admissible flow through it before any other node is considered, which can expose an entirely different tree structure.

% TODO(revision): Equation (\ref{eq:op_boost}) reflects the implementation, in which the boosted node receives
% max(pi) + 1 rather than Z. The relative order -- which is all the decoder consumes -- is correct either way, so no
% result is affected, but the image of pi' is then {1,...,Z-1} U {Z+1} rather than {1,...,Z}, which is inconsistent
% with the bijection stated in Section \ref{subsubsec:op_notation}. Setting the boosted value to Z instead of
% max(pi) + 1 restores the invariant and changes nothing else. Fix in the code, then simplify this definition.

\paragraph{Inactive-Active Exchange with Nearest Neighbor (IA-ETN).} \textit{Precondition:} $\mathcal{A} \neq \emptyset$ and $\mathcal{I} \neq \emptyset$. Draw $v \in \mathcal{I}$
uniformly and let
%
\begin{equation}
\label{eq:op_iaetn}
u^{*} \in \arg\min_{u \in \mathcal{A}} \; \bigl| \mathrm{pos}(u) - \mathrm{pos}(v) \bigr| ,
\end{equation}
%
ties being broken by the lowest position. The operator then exchanges $\pi(v)$ and $\pi(u^{*})$ as in Equation~\eqref{eq:op_iaswap}. Replacing the uniform draw of the active partner by
the positionally nearest one makes this a localized counterpart of the Inactive-Active Swap: it probes the boundary between $\mathcal{A}$ and $\mathcal{I}$, testing whether a facility
that is nearly good enough to be used should displace a marginally used one, rather than attempting an arbitrary global rearrangement.

% ----------------------------------------------------------
\subsubsection{Cost and Applicability}
\label{subsubsec:op_cost}

All four operators run in $O(Z_s)$ time, dominated in the first, third and fourth cases by the scan required to recover $\mathcal{A}_s$ and $\mathcal{I}_s$ from $\alpha_s$. This cost is
negligible against that of the re-decoding each move triggers, so the number of objective evaluations, rather than the number of moves, is the meaningful measure of computational
effort and is what we budget in Section~\ref{subsubsec:shared}.

Two properties should be noted. First, the operators are defined only where the decoder has been run, since $\alpha_s$ is part of its output; a move can therefore never be applied to a
chromosome that has not been evaluated. Second, all four are structure-preserving in the sense that they map a valid priority assignment to a valid one, so no repair step is required
and every chromosome reachable by the search decodes to a feasible solution.

% ----------------------------------------------------------
\subsection{The VNS Algorithm}
\label{subsec:vns}

The proposed algorithm is a hybrid metaheuristic that enhances the standard Variable Neighborhood Search (VNS) \citep{hansen2010} by integrating Variable Neighborhood Descent (VND) 
for intensification and a Simulated Annealing (SA) acceptance criterion. This hybridization addresses the limitations of pure descent methods in the highly constrained landscape of the 
pistachio supply chain. The procedure is outlined in Algorithm \ref{algoVNS}.

The intensification phase employs a Variable Neighborhood Descent (VND) strategy rather than a simple local search. In this step, the algorithm iterates sequentially through the
entire set of neighborhood structures. Whenever an improvement is found with any operator, the search restarts from the first operator.

% Reviewer~2, comment~4: the VND uses sampled rather than exhaustive best improvement. This was not stated before and
% it materially changes what "local optimum" means here, so it is made explicit.
One point deserves emphasis, since it qualifies the sense in which the VND returns a local optimum. The neighborhoods of Section~\ref{subsec:neighborhoods} are not enumerated
exhaustively. Each operator is stochastic, and evaluating its full neighborhood would be prohibitive: the Inactive-Active Swap alone admits $|\mathcal{A}_s| \cdot |\mathcal{I}_s|$
candidates per stage, each requiring a complete re-decoding. Instead, the descent step draws a fixed sample of $\eta = 45$ independent candidates from the current operator, evaluates
them, and adopts the best if it improves on the incumbent. The procedure is therefore a \emph{sampled} best-improvement descent, and the solution it returns is a local optimum with
respect to the sampled neighborhoods rather than a certified local optimum with respect to the complete ones. The sample size $\eta$ is held constant across operators and instance
sizes, and it enters the computational budget directly, since each of the $\eta$ candidates consumes one objective evaluation.

% TODO(revision): the trajectory-based competitor uses a different sample size in its own descent step (15 against
% the 45 used here), which is an unjustified asymmetry of exactly the kind Reviewer~4 comment~6 objects to. Harmonise
% the two before the experiments are re-run, then state the common value here and in Section
% \ref{subsubsec:shared}. Until that is done this paragraph must not claim that eta is shared across algorithms.

To prevent stagnation in local optima, we replace the standard deterministic acceptance criterion with a probabilistic Simulated Annealing mechanism. This probabilistic acceptance 
is particularly valuable in highly constrained problems like supply chain networks, where the solution space contains numerous local optima separated by infeasible regions. 
A new solution generated by the VND phase is accepted as the current solution if it improves the objective function or, if it is worse, with a probability given by the 
Boltzmann factor $P = e^{-\Delta / T}$, where $\Delta$ is the deterioration in solution quality and $T$ is the current temperature. This allows the search to traverse valleys 
in the fitness landscape and explore promising regions that would be rejected by pure descent methods.

Finally, the shaking phase applies a perturbation to the current solution to escape the basin of attraction of the local optimum. The magnitude of this perturbation is adaptive, 
defined in our implementation as 7\% of the total decision variables in the chromosome. This value was determined through preliminary experiments testing perturbation strengths 
ranging from 3\% to 15\% on representative instances. Strengths below 5\% resulted in insufficient diversification, causing the algorithm to revisit previously explored regions, 
while values above 10\% caused excessive disruption, degrading solution quality. The 7\% value provided the best balance between escaping local optima and maintaining solution 
structure. The specific operator used for shaking changes cyclically, resetting to the first operator upon a successful acceptance and 
moving to the next operator if the candidate solution is rejected.

\begin{algorithm}
    \caption{Hybrid VNS with SA Acceptance}
    \label{algoVNS}
    \begin{algorithmic}[1]
    \Require Initial solution $x$, Operators set $\mathcal{N}$, Max evaluations $N_{max}$, Initial Temp $T_0$, Cooling rate $\alpha$
    \State \textbf{Output:} Best solution found $x^*$
    
    \State $x_{curr} \leftarrow x$; \quad $x^* \leftarrow x$
    \State $n_{eval} \leftarrow 1$; \quad $T \leftarrow T_0$
    \State $k \leftarrow 0$ \Comment{Operator index for shaking}

    \While{$n_{eval} < N_{max}$}
        
        \State \textit{// Phase 1: Intensification (VND)}
        \State $x_{new} \leftarrow \text{VND\_LocalSearch}(x_{curr}, \mathcal{N})$ 
        \State Update $n_{eval}$
        
        \State \textit{// Phase 2: Update Global Best}
        \If{$f(x_{new}) < f(x^*)$}
            \State $x^* \leftarrow x_{new}$
        \EndIf

        \State \textit{// Phase 3: Acceptance Criteria (SA)}
        \State $\Delta \leftarrow f(x_{new}) - f(x_{curr})$
        \If{$\Delta \le 0$ \textbf{or} $\text{Random}(0,1) < e^{-\Delta / T}$}
            \State $x_{curr} \leftarrow x_{new}$
            \State $k \leftarrow 0$ \Comment{Reset shaking operator on acceptance}
        \Else
            \State $k \leftarrow (k + 1) \pmod{|\mathcal{N}|}$ \Comment{Next shaking operator}
        \EndIf

        \State \textit{// Phase 4: Perturbation and Cooling}
        \State $T \leftarrow T \times \alpha$
        \State $x_{curr} \leftarrow \text{Shake}(x_{curr}, \mathcal{N}[k])$
        
    \EndWhile
    \end{algorithmic}
\end{algorithm}

The VND phase applies the four operators defined in Section~\ref{subsec:neighborhoods} in the following fixed sequence: (1) Active Reversion, (2) Inactive-Active Swap, (3) Inactive Boost,
and (4) Inactive-Active Exchange two Neighbors (IA-ETN). This specific ordering follows a strategic progression from conservative to aggressive exploration. First, Active Reversion fine-tunes the existing solution topology by reordering active nodes, 
exploring the current structure before considering more disruptive changes. Second, Inactive-Active Swap introduces global diversification by randomly exchanging nodes between 
active and inactive sets, testing whether alternative facility configurations might be superior. Third, Inactive Boost applies the most aggressive perturbation, forcing a previously unused node 
to maximum priority—this operator is crucial for escaping situations where good facilities are trapped at low priorities. Finally, IA-ETN provides localized diversification by testing the boundary 
between active and inactive sets, swapping neighboring nodes to explore incremental topology changes. This gradual escalation from refinement to disruption ensures that the algorithm exhausts 
local improvements before resorting to more radical modifications, maximizing the effectiveness of the VND phase.

These operators are applied to the priority-based chromosome during the VND phase, with each move triggering a complete re-decoding using the procedure described in 
Algorithms \ref{algoDecode1} and \ref{algoDecode2}.


% ----------------------------------------------------------------------------------------------------------------------
\section{Computational Experiments}
\label{sec:experiments}

This section evaluates the performance of the proposed VNS approach. Sections~\ref{subsec:setup} to~\ref{subsec:stats_method} describe the computational environment, the
benchmark instances, the components shared by all compared algorithms, and the statistical methodology. Section~\ref{subsec:main_comparison} then benchmarks the proposed VNS
against a Genetic Algorithm (GA), an Iterated Local Search (ILS), and the exact solver. Section~\ref{subsec:operators_analysis} isolates the contribution of the problem-specific
neighborhood structures by comparing them against classical permutation operators, and Section~\ref{subsec:ablation} reports an ablation study that separates the contribution
of each individual component of the proposed algorithm.

% ------------------------------------------------------------

\subsection{Experimental Setup}
\label{subsec:setup}

% TODO(revision): Reviewer~2, comment~9 asks for a more recent Gurobi release and longer time limits on the large
% instances. Update the version string and the hardware description once the experiments are re-run.
The algorithms were implemented in Python and executed on a workstation with an Intel Xeon E3-1240 V2 processor (8 cores) and 32 GB of RAM running Ubuntu 24.04 LTS. The exact method
was solved using Gurobi Optimizer v11.0.

% ------------------------------------------------------------

\subsection{Benchmark Instances}
\label{subsec:instances}

% =====================================================================================================================
% TODO(revision): item (a) below is DONE -- the chromosome length description is corrected here and derived in
% Section \ref{subsubsec:chromosome}. Two issues remain, both requiring the instances to be regenerated:
%   (b) Reviewer~2, comment~7: a single instance per size is not enough to assess robustness. Report the number of
%       independent instances generated per size and aggregate results over them.
%   (c) Reviewer~2, comment~8: the instances must be published in the GitHub repository, not on external storage.
%       NOTE: before publishing, the N=1600 instance must be regenerated. The loader currently resolves it to the
%       same file as N=800, which is why both sizes report identical objective values in every results table.
% =====================================================================================================================
The test instances were generated following the methodology proposed by \cite{rajabi2023}. Each instance is characterized by equal-sized entities across all echelons
(i.e., $I = J = K = E = S = Q = N_1 = N_2 = N_3 = M = N$), with instance sizes of $N \in \{10, 30, 100, 200, 400, 800, 1600\}$. Under this convention, the chromosome length of
Equation~\eqref{eq:chromo_length} reduces to $L = 17N$, so the priority vectors range from $170$ decision variables for $N = 10$ to $27{,}200$ for $N = 1600$. We stress that this
growth is linear rather than quadratic, because priorities are attached to nodes rather than to arcs; the number of \emph{arcs} in the underlying network does grow quadratically,
but those arcs are induced by the decoder rather than enumerated in the genotype.

All problem parameters (facility costs, production costs, transportation costs, capacities, demands, and conversion rates) were randomly generated using uniform distributions
with ranges identical to those specified in Table 7 of \cite{rajabi2023}. Complete parameter specifications and the generated instances are publicly available
in the project repository to ensure full reproducibility.

% ------------------------------------------------------------

\subsection{Algorithm Configurations}
\label{subsec:configurations}

\subsubsection{Shared Components}
\label{subsubsec:shared}

% =====================================================================================================================
% TODO(revision): Addresses Reviewer~1 comment~2, Reviewer~2 comment~6, and Reviewer~4 comment~6. All three question
% whether the comparison is fair. It is, and the implementation already guarantees it -- the manuscript simply never
% says so. This subsection must state explicitly, and ideally as a table of shared vs. algorithm-specific components,
% that VNS, GA and ILS all share:
%   - the same integer priority encoding (the same Solution class)
%   - the same decoding procedure (Algorithms 1-2), hence the same genotype-to-phenotype map
%   - the same objective evaluation function
%   - the same constraint handling: feasibility by construction in the decoder, with no penalty term and no repair
%   - the same deterministic initial solution
%   - the same stopping criterion and the same evaluation-counting semantics (one count per decode)
%   - the same instances and the same number of independent runs
% Reviewer~2 comment~6 in particular assumes the GA is terminated by generations. It is not: it uses the same
% evaluation counter and the same budget as the trajectory-based methods. This should be stated as a correction.
% =====================================================================================================================

\textbf{[TO BE WRITTEN]} \textit{Table of shared versus algorithm-specific components, establishing that the three metaheuristics differ only in their search strategy.}

To ensure a fair comparison across all metaheuristics, each algorithm was allocated an identical computational budget: $N_{max} = 10n$, where $n$ represents the total number of
decision variables in the priority-based encoding. This budget scales linearly with problem size, ensuring equitable evaluation across all instances.

All metaheuristics (VNS, GA, ILS) were executed 30 times for each instance to ensure statistical robustness.

\subsubsection{Algorithm-Specific Parameters}
\label{subsubsec:params}

% TODO(revision): Reviewer~1, comment~2 and Reviewer~4, comment~6 ask for fuller descriptions of the compared
% algorithms. Add ILS pseudocode as an algorithm environment, complete the GA description, and report the
% configurations that were tested and discarded for each competitor so that the reported baseline is visibly the
% best-performing one rather than an arbitrary choice.
The specific parameter settings for each algorithm are as follows:

\begin{itemize}
    \item \textbf{Genetic Algorithm (GA):} The GA uses a population size of $P = \lfloor\sqrt{n}\rfloor$, where $n$ is the total number of decision variables in the priority encoding,
    motivated by population-sizing theory suggesting that required population grows roughly with the square root of problem size. The algorithm employs a hybrid crossover operator (probability 0.9)
    that randomly alternates between two strategies with equal probability: (i) segment-level crossover, which exchanges entire chromosome segments ($S_1$ through $S_8$) between parents, and
    (ii) intra-segment crossover, which applies Partially Mapped Crossover (PMX) within each individual segment to preserve permutation validity while promoting diversity. This hybrid approach
    balances global exploration (segment exchange) with local refinement (intra-segment recombination). Mutation probability is set to 0.1, and binary tournament selection is used for parent
    selection, following widely established GA practices \citep{hussain2022genetic}.

    \item \textbf{Iterated Local Search (ILS):} The ILS alternates between local search and perturbation phases. The perturbation strength scales with instance size, and the algorithm
    restarts when no improvement is observed after 10 consecutive non-improving iterations \citep{tinos2024iterated}. Local search employs the problem-specific "Inactive–Active Swap" operator.

    \item \textbf{Variable Neighborhood Search (VNS):} The proposed VNS uses initial temperature $T_0 = 100$ and cooling rate $\alpha = 0.995$, which are standard parameters widely
    adopted in Simulated Annealing literature \citep{kirkpatrick1983optimization}, providing effective balance between exploration and exploitation. The perturbation strength is set to 7\% of the
    chromosome length, a value empirically determined as described in Section~\ref{subsec:vns}. The VND phase cycles sequentially through four problem-specific operators: (1) Active Reversion,
    (2) Inactive-Active Swap, (3) Inactive Boost, and (4) IA-ETN. Upon finding an improvement with any operator, the VND restarts from the first operator.

    \item \textbf{Exact Solver (Gurobi):} For the three largest instances (400, 800, and 1600), the solver was configured with a time limit of 3,600 seconds (1 hour).
    For smaller instances, no time limit was imposed, allowing the solver to run until proven optimality. For instances where the optimal solution could not be found within
    the time limit, the best feasible solution found ($\text{Incumbent}$) was recorded.
\end{itemize}

% ------------------------------------------------------------

\subsection{Statistical Methodology}
\label{subsec:stats_method}

% TODO(revision): add a multiple-comparison correction (Holm) and a non-parametric effect size measure
% (Vargha-Delaney A12 or Cliff's delta) once the experiments are re-run. Reporting p-values alone for fourteen
% pairwise tests is a standing vulnerability even though the current p-values are small enough that no correction
% would change any conclusion.
The same statistical procedure is applied to every pairwise comparison reported in this section. Since parametric tests (such as the t-test) assume normally distributed data,
we first applied the Shapiro-Wilk test with significance level $\alpha = 0.05$ to check this assumption. When both samples passed the normality test ($p > 0.05$),
we used the parametric t-test, which offers higher statistical power. When either sample failed the normality test ($p \le 0.05$), we used the non-parametric Mann-Whitney
U test, which makes no distributional assumptions. All hypotheses are one-tailed, testing whether the first method attains a smaller (better) objective function value than
the second.

% ------------------------------------------------------------
\subsection{Comparative Analysis}
\label{subsec:main_comparison}

In this section, we provide a comprehensive benchmark of the proposed VNS against the Genetic Algorithm (GA), Iterated Local Search (ILS), and the exact solver (Gurobi). 
Fig. \ref{fig:comparison_boxplots} presents the boxplots for all methods across the tested instances. The red dashed line represents the optimal solution (or best lower bound) found by Gurobi.

\begin{figure*}[htbp]
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_10.png}
        \caption{Instance 10}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_30.png}
        \caption{Instance 30}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_100.png}
        \caption{Instance 100}
    \end{subfigure}
    
    \vspace{1em}
    
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_200.png}
        \caption{Instance 200}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_400.png}
        \caption{Instance 400}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_800.png}
        \caption{Instance 800}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots_1600.png}
        \caption{Instance 1600}
    \end{subfigure}
    
    \caption{Comparison of VNS, ILS, and GA across all instance sizes. The red dashed line indicates the reference solution (Gurobi optimum or best incumbent).}
    \label{fig:comparison_boxplots}
\end{figure*}

\subsubsection{Performance Comparison}
Table \ref{tab:descriptive_stats} summarizes the average objective function values and the relative gap to the optimal solution for each method.

For the smallest instance (10), the ILS achieves the best average performance, surpassing the VNS by 2.21\%. This is expected, as simple local search strategies often converge 
faster in very small search spaces where deep exploration is unnecessary. For Instance 30, the performance of VNS and ILS is statistically indistinguishable.

However, a clear trend emerges from Instance 100 onwards. The VNS consistently outperforms both ILS and GA, with the performance gap widening as the problem complexity increases. 
For the largest instances (800 and 1600), the VNS maintains a gap to the optimum of approximately 3.5\%, whereas the ILS gap grows to 4.7\% and the GA gap exceeds 10\%. 
This demonstrates the superior scalability of the proposed approach.

For the three largest instances (400, 800, and 1600), Gurobi was subject to a one-hour time limit to prevent excessive computational costs. Instance 400 reached proven optimality 
within this limit. However, Instances 800 and 1600 exhausted the time budget without proving optimality, returning the same best incumbent solution value of $5.49 \times 10^8$ for both. 
These values represent the best feasible solutions found (upper bounds) rather than proven optimal solutions, as the optimality gap had not closed. For smaller instances (10, 30, 100, and 200), 
Gurobi ran without time constraints and successfully proved optimality.

\begin{table*}[ht]
    \centering
    \caption{Descriptive Statistics: Average Objective Function values and Gap to Reference Solution (\%). Note: Instances 10-200 were solved to proven optimality by Gurobi without time constraints. 
    Instance 400 reached proven optimality within the 1-hour time limit. For instances 800 and 1600, Gurobi reached the time limit without proving optimality; 
    gaps are calculated relative to the best incumbent solution rather than a proven optimum.}
    \label{tab:descriptive_stats}
    \resizebox{\textwidth}{!}{
    \begin{tabular}{rcccccccc}
        \toprule
        & \textbf{Reference} & \multicolumn{3}{c}{\textbf{Average Objective Function}} & \multicolumn{3}{c}{\textbf{Gap to Reference (\%)}} \\
        \cmidrule(lr){3-5} \cmidrule(lr){6-8}
        Inst. & Gurobi (Opt/Bound) & Proposed VNS & ILS & GA & VNS & ILS & GA \\
        \midrule
        10 & $7.32 \times 10^6$ & $7.62 \times 10^6$ & $\mathbf{7.46 \times 10^6}$ & $7.82 \times 10^6$ & 4.17\% & \textbf{1.92\%} & 6.89\% \\
        30 & $2.00 \times 10^7$ & $\mathbf{2.05 \times 10^7}$ & $\mathbf{2.05 \times 10^7}$ & $2.11 \times 10^7$ & \textbf{2.65\%} & 2.76\% & 5.39\% \\
        100 & $7.06 \times 10^7$ & $\mathbf{7.29 \times 10^7}$ & $7.34 \times 10^7$ & $7.67 \times 10^7$ & \textbf{3.28\%} & 3.97\% & 8.59\% \\
        200 & $1.38 \times 10^8$ & $\mathbf{1.42 \times 10^8}$ & $1.44 \times 10^8$ & $1.49 \times 10^8$ & \textbf{3.40\%} & 4.48\% & 8.48\% \\
        400 & $2.71 \times 10^8$ & $\mathbf{2.80 \times 10^8}$ & $2.83 \times 10^8$ & $2.96 \times 10^8$ & \textbf{3.27\%} & 4.42\% & 9.02\% \\
        800 & $5.49 \times 10^8$ & $\mathbf{5.68 \times 10^8}$ & $5.75 \times 10^8$ & $6.01 \times 10^8$ & \textbf{3.54\%} & 4.72\% & 9.53\% \\
        1600 & $5.49 \times 10^8$ & $\mathbf{5.68 \times 10^8}$ & $5.74 \times 10^8$ & $6.04 \times 10^8$ & \textbf{3.50\%} & 4.65\% & 10.13\% \\
        \bottomrule
    \end{tabular}
    }
\end{table*}

\subsubsection{Statistical Validation}
To rigorously validate the performance differences, we applied the statistical methodology described in Section~\ref{subsec:stats_method} with $\alpha = 0.05$ for all tests. 
Table \ref{tab:detailed_stats} presents the normality checks ($p_{SW}$) and hypothesis testing results for all pairwise comparisons between VNS and its competitors.

The hypothesis tested was $H_1: \mu_{VNS} < \mu_{Competitor}$ (one-tailed). As shown in the table, the data distribution characteristics vary with instance size, 
requiring different statistical treatments (parametric t-test when normality holds, non-parametric Mann-Whitney U test otherwise).

\begin{table*}[ht]
    \centering
    \caption{Detailed Statistical Analysis: Normality Checks and Hypothesis Testing ($\alpha=0.05$).}
    \label{tab:detailed_stats}
    \resizebox{\textwidth}{!}{
        \begin{tabular}{rcccccccc}
            \toprule
            & \multicolumn{3}{c}{\textbf{Normality Check ($p_{SW}$)}} & \multicolumn{5}{c}{\textbf{Hypothesis Testing ($H_1: VNS < Competitor$)}} \\
            \cmidrule(lr){2-4} \cmidrule(lr){5-9}
            Inst. & VNS & ILS & GA & Comparison & Normality? & Test Used & p-value & \textbf{Conclusion} \\
            \midrule
            \multirow{2}{*}{10} & \multirow{2}{*}{1.70e-11} & \multirow{2}{*}{5.41e-01} & \multirow{2}{*}{3.74e-07} 
            & VNS vs ILS & No & Mann-Whitney & 9.98e-01 & No Sig. Diff. \\
            & & & & VNS vs GA & No & Mann-Whitney & 1.09e-07 & VNS Superior \\
            \midrule
            \multirow{2}{*}{30} & \multirow{2}{*}{6.26e-02} & \multirow{2}{*}{6.64e-01} & \multirow{2}{*}{4.62e-01} 
            & VNS vs ILS & Yes & t-test & 2.94e-01 & No Sig. Diff. \\
            & & & & VNS vs GA & Yes & t-test & 1.83e-12 & VNS Superior \\
            \midrule
            \multirow{2}{*}{100} & \multirow{2}{*}{5.12e-01} & \multirow{2}{*}{6.16e-01} & \multirow{2}{*}{3.46e-05} 
            & VNS vs ILS & Yes & t-test & 7.94e-06 & VNS Superior \\
            & & & & VNS vs GA & No & Mann-Whitney & 1.51e-11 & VNS Superior \\
            \midrule
            \multirow{2}{*}{200} & \multirow{2}{*}{4.34e-02} & \multirow{2}{*}{8.77e-01} & \multirow{2}{*}{6.67e-02} 
            & VNS vs ILS & No & Mann-Whitney & 3.06e-10 & VNS Superior \\
            & & & & VNS vs GA & No & Mann-Whitney & 1.51e-11 & VNS Superior \\
            \midrule
            \multirow{2}{*}{400} & \multirow{2}{*}{2.96e-01} & \multirow{2}{*}{2.95e-01} & \multirow{2}{*}{6.25e-01} 
            & VNS vs ILS & Yes & t-test & 1.15e-20 & VNS Superior \\
            & & & & VNS vs GA & Yes & t-test & 1.03e-20 & VNS Superior \\
            \midrule
            \multirow{2}{*}{800} & \multirow{2}{*}{7.11e-01} & \multirow{2}{*}{8.82e-01} & \multirow{2}{*}{4.39e-01} 
            & VNS vs ILS & Yes & t-test & 4.78e-20 & VNS Superior \\
            & & & & VNS vs GA & Yes & t-test & 1.71e-24 & VNS Superior \\
            \midrule
            \multirow{2}{*}{1600} & \multirow{2}{*}{3.56e-01} & \multirow{2}{*}{1.91e-01} & \multirow{2}{*}{1.27e-01} 
            & VNS vs ILS & Yes & t-test & 1.38e-17 & VNS Superior \\
            & & & & VNS vs GA & Yes & t-test & 5.97e-23 & VNS Superior \\
            \bottomrule
        \end{tabular}
    }
\end{table*}

The analysis confirms two distinct performance regimes. In small instances (10-30), the simple ILS is highly efficient, and VNS provides no statistical advantage (p-value $> 0.05$). 
However, a transition occurs at Instance 100. From this point onward, the VNS is statistically superior to both ILS and GA ($p < 10^{-5}$). Notably, in Instance 200, 
the VNS distribution deviated from normality ($p=4.34 \times 10^{-2}$), requiring the non-parametric Mann-Whitney test, which still confirmed the result with high confidence.

\subsubsection{Performance Regime Analysis}
The observed performance stratification reveals important insights about algorithm selection based on problem scale. For small instances (10-30 nodes), the solution space 
is relatively compact, and simple greedy descent strategies employed by ILS converge rapidly to near-optimal solutions. In these cases, the overhead of VNS's multi-neighborhood 
framework and SA acceptance mechanism provides no advantage, since the problem is simple enough that basic local search suffices. The VND cycling through multiple operators and the 
probabilistic acceptance of worse solutions become unnecessary computational expenses when the local optimum basin is shallow and easily escapable.

However, as instance size grows beyond 100 nodes, the combinatorial explosion creates a rugged fitness landscape with numerous deep local optima separated by large barriers. 
Here, the VNS's sophisticated diversification mechanisms (strategic neighborhood sequencing, SA-based hill climbing, and adaptive perturbation) become essential. The problem-specific 
operators exploit structural properties of the supply chain that are invisible to classical operators, enabling the algorithm to navigate 
the active/inactive node boundary more effectively. The ILS, constrained to a single neighborhood operator, lacks this structural awareness and becomes trapped in inferior local optima. 
Similarly, the GA's population-based search suffers from the curse of dimensionality: as chromosome length grows from $170$ to $27{,}200$ variables, maintaining population diversity while
guiding convergence becomes increasingly difficult, resulting in premature convergence to suboptimal regions.

\subsubsection{Convergence Analysis}
Figure \ref{fig:convergence_plots} illustrates the evolutionary boxplots for all three methods. The ILS (left column) demonstrates a consistent behavior with relatively low 
variance (small box sizes) throughout the execution. However, despite this stability, it fails to reach the deeper regions found by the VNS, stopping at higher objective function values.

The GA (right column) struggles with the scale of the problem. In larger instances (800 and 1600), its convergence curve is still strictly descending at the end of the 
computational budget, indicating that it requires significantly more evaluations to compete with the trajectory-based methods.

The Proposed VNS (middle column) offers the best trade-off. In the largest instances, it starts with a broader search distribution (driven 
by the Simulated Annealing acceptance criteria and the specific neighborhood operators) allowing it to filter through various basins of attraction. Crucially, while the ILS 
stabilizes early at a sub-optimal level, the VNS sustains a steep descent trajectory for longer, ultimately securing the lowest objective function values with high 
reliability across all 30 runs.

\begin{figure*}[htbp]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_10.png}
        \caption{Instance 10}
    \end{subfigure}
    
    \vspace{0.5em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_30.png}
        \caption{Instance 30}
    \end{subfigure}
    
    \vspace{0.5em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_100.png}
        \caption{Instance 100}
    \end{subfigure}
    
    \vspace{0.5em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_200.png}
        \caption{Instance 200}
    \end{subfigure}
    
    \vspace{0.5em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_400.png}
        \caption{Instance 400}
    \end{subfigure}
    
    \vspace{0.5em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_800.png}
        \caption{Instance 800}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{./convergence_1600.png}
        \caption{Instance 1600}
    \end{subfigure}
    
    \caption{Convergence profiles of VNS, ILS, and GA across all instance sizes. Boxplots show the distribution of objective values at different 
    execution stages (as percentage of total evaluations), with median lines connecting the boxes. The VNS demonstrates sustained improvement 
    throughout execution, while GA plateaus earlier in large instances.}
    \label{fig:convergence_plots}
\end{figure*}

% ------------------------------------------------------------

\subsection{Assessment of Neighborhood Structures}
\label{subsec:operators_analysis}

To validate the contribution of the problem-specific neighborhood structures, we conducted a comparative study against a baseline "Standard VNS". The Standard VNS employs only the 
classical permutation operators found in the literature: \textit{Swap}, \textit{Reversion}, \textit{Insertion}, and \textit{Slide} \citep{gendreau2010}.

\subsubsection{Statistical Analysis}
Following the procedure defined in Section~\ref{subsec:stats_method}, the hypothesis tested here was that the Proposed VNS provides a smaller (better) objective
function value than the Standard VNS ($H_1: \mu_{Proposed} < \mu_{Standard}$).

Table \ref{tab:stats_vns} details the p-values obtained. For small instances (10 and 30), the p-values ($> 0.05$) indicate that there is no statistically significant difference 
between the Proposed VNS and the Standard VNS. The null hypothesis ($H_0: \mu_{Proposed} = \mu_{Standard}$) cannot be rejected, meaning both approaches perform comparably in these simple landscapes. 
However, for all instances of size 100 and above, the p-values are effectively zero ($p < 0.0001$), confirming that the Proposed VNS is statistically superior with high confidence.

\begin{table}[h]
    \centering
    \small
    \caption{Statistical tests comparing Proposed VNS vs. Standard VNS. (S-W = Shapiro-Wilk p-value).}
    \label{tab:stats_vns}
    \begin{tabular}{rccclc}
        \toprule
        Inst. & S-W (Proposed) & S-W (Standard) & Test & p-value & Result \\
        \midrule
        10 & $1.70 \times 10^{-11}$ & 0.0008 & Mann-W. & 1.0000 & No Sig. Diff. \\
        30 & 0.0626 & 0.0493 & Mann-W. & 0.9811 & No Sig. Diff. \\
        100 & 0.5115 & 0.5658 & t-test & $3.64 \times 10^{-6}$ & Proposed Superior \\
        200 & 0.0434 & 0.5143 & Mann-W. & $2.25 \times 10^{-11}$ & Proposed Superior \\
        400 & 0.2958 & 0.6396 & t-test & $2.29 \times 10^{-32}$ & Proposed Superior \\
        800 & 0.7107 & 0.1993 & t-test & $2.76 \times 10^{-27}$ & Proposed Superior \\
        1600 & 0.3556 & 0.3454 & t-test & $5.89 \times 10^{-31}$ & Proposed Superior \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Performance Magnitude}
Table \ref{tab:performance_vns} quantifies the difference in solution quality. While standard operators are sufficient for small problems, their performance degrades as the search space expands. 
The proposed operators demonstrate a clear scalability advantage, with the performance gap widening in favor of the Proposed VNS as the instance size increases, reaching nearly 5\% improvement 
in the largest instances.

\begin{table}[h]
    \centering
    \caption{Average Objective Function comparison. Gap positive indicates Proposed VNS is better.}
    \label{tab:performance_vns}
    \begin{tabular}{rccc}
        \toprule
        Instance & Avg. Obj (Proposed) & Avg. Obj (Standard) & Gap (\%) \\
        \midrule
        10 & $7.62 \times 10^6$ & $7.38 \times 10^6$ & -3.05\% \\
        30 & $2.05 \times 10^7$ & $2.04 \times 10^7$ & -0.44\% \\
        100 & $7.29 \times 10^7$ & $7.34 \times 10^7$ & +0.71\% \\
        200 & $1.42 \times 10^8$ & $1.44 \times 10^8$ & +1.45\% \\
        400 & $2.80 \times 10^8$ & $2.87 \times 10^8$ & +2.52\% \\
        800 & $5.68 \times 10^8$ & $5.94 \times 10^8$ & +4.58\% \\
        1600 & $5.67 \times 10^8$ & $5.95 \times 10^8$ & +4.83\% \\
        \bottomrule
    \end{tabular}
\end{table}

Figure \ref{fig:vns_boxplots} illustrates these distributions visually. The boxplots confirm that for $N \ge 100$, the Proposed VNS not only achieves better median values but also exhibits 
lower variance, indicating greater robustness.

\begin{figure*}[htbp]
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_10.png}
        \caption{Instance 10}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_30.png}
        \caption{Instance 30}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_100.png}
        \caption{Instance 100}
    \end{subfigure}
    
    \vspace{1em}
    
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_200.png}
        \caption{Instance 200}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_400.png}
        \caption{Instance 400}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_800.png}
        \caption{Instance 800}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}[b]{0.3\textwidth}
        \includegraphics[width=\textwidth]{./boxplots2_1600.png}
        \caption{Instance 1600}
    \end{subfigure}
    
    \caption{Distribution of objective function values for Proposed VNS (left boxplot) vs. Standard VNS (right boxplot) across all instance sizes. The red dashed line represents the optimal solution found by branch-and-bound (Gurobi). The proposed operators demonstrate superior scalability.}
    \label{fig:vns_boxplots}
\end{figure*}

\subsubsection{Convergence Behavior Analysis}
Figure \ref{fig:vns_convergence} provides temporal insights into the search dynamics by showing the distribution of best-so-far objective values at different execution stages 
(12.5\%, 25\%, 37.5\%, etc.). The plots reveal a fundamental behavioral difference between the two VNS variants.

The Standard VNS (labeled "2", orange line) exhibits a "cliff-like" convergence profile. It descends rapidly in the first 12.5\% of execution, then immediately plateaus 
with extremely tight distributions (small boxes). While this indicates high repeatability across runs, it signals premature convergence: the algorithm becomes trapped 
in the first local optimum encountered and lacks sufficient diversification mechanisms to escape, effectively stagnating for the remaining 85\% of the computational budget.

In contrast, the Proposed VNS (labeled "1", blue boxes) displays substantially wider interquartile ranges during the initial 25\% of execution, particularly visible 
in instances $N \ge 200$. This variance is not a weakness but validates the effectiveness of the proposed diversification operators. 
By forcing transitions between active and inactive facility configurations, these operators enable the algorithm to traverse multiple basins of attraction, temporarily 
increasing solution variance while systematically filtering inferior local optima. Critically, as execution progresses beyond 50\%, the distributions tighten considerably, 
demonstrating that the algorithm successfully identifies and converges toward high-quality regions of the solution space. The final distributions (at 100\% execution) show 
medians consistently 4-5\% lower than the Standard VNS. Thus, the initial exploration phase, though appearing less stable, constitutes a strategic investment that yields 
superior solution quality in the long run.

\begin{figure*}[htbp]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_10.png}
        \caption{Instance 10}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_30.png}
        \caption{Instance 30}
    \end{subfigure}
    
    \vspace{1em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_100.png}
        \caption{Instance 100}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_200.png}
        \caption{Instance 200}
    \end{subfigure}
    
    \vspace{1em}
    
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_400.png}
        \caption{Instance 400}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_800.png}
        \caption{Instance 800}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}[b]{0.48\textwidth}
        \includegraphics[width=\textwidth]{./convergence2_1600.png}
        \caption{Instance 1600}
    \end{subfigure}
    
    \caption{Convergence curves comparing Proposed VNS (left/blue) vs. Standard VNS (right/orange) across all instance sizes. The x-axis represents the number of objective function evaluations, while the y-axis shows the best solution found up to that point. The Proposed VNS demonstrates faster convergence and better final solutions, particularly for larger instances.}
    \label{fig:vns_convergence}
\end{figure*}


% ------------------------------------------------------------

\subsection{Ablation Study}
\label{subsec:ablation}

% =====================================================================================================================
% TODO(revision): Addresses Reviewer~2 comment~1 ("no substantive methodological advancement beyond existing
% approaches"), Reviewer~4 comment~1 (the operators are structured variants of known moves) and Reviewer~4 comment~6
% (the ILS baseline is structurally weaker, so the source of the improvement cannot be attributed). All three reduce
% to the same question: where does the gain actually come from? Only an ablation can answer it.
%
% Variants to run, isolating one component at a time:
%   1. VND + proposed operators + strict-improvement acceptance
%   2. VND + proposed operators + normalised SA acceptance
%   3. VND + classical operators (Swap, Reversion, Insertion, Slide) -- the existing "Standard VNS"
%   4. Single-neighborhood VNS using Inactive-Active Swap only -- separates "more neighborhoods" from
%      "better neighborhoods"
%   5. Strengthened ILS given the four proposed operators under the same budget -- this is the decisive one for
%      Reviewer~4 comment~6. If VNS still wins against an ILS with equal access to the operators, the objection
%      falls. If it does not, the contribution is the operators rather than the architecture, and the paper should
%      be framed that way.
%
% Note on variant 2: the acceptance criterion must be corrected before this subsection can be written. Reviewer~4
% comment~5 is correct that with objective values of order 1e7-1e8 and T0 = 100, exp(-delta/T) is numerically zero,
% so the search behaves as deterministic descent. Section~\ref{subsec:vns} must describe the corrected mechanism,
% not the current one.
% =====================================================================================================================

\textbf{[TO BE WRITTEN]} \textit{Ablation over the five variants listed above, attributing the observed performance to individual algorithmic components.}


% ------------------------------------------------------------

\subsection{Managerial Implications}
\label{subsec:managerial}

% TODO(revision): moved here from the Conclusion, which Reviewer~2 comment~11 found overly long and too detailed.
% Two follow-ups once the experiments are re-run: the gap figure quoted below is a placeholder, and the phrase
% "near-optimal" must be reconciled with Reviewer~4 comment~7 -- where Gurobi did not prove optimality the
% comparison is against a best incumbent, not against an optimum.
From a practical standpoint, our results suggest a tiered decision framework for supply chain managers in the pistachio industry based on network complexity. 
Networks with fewer than 100 facilities per echelon should employ commercial solvers (Gurobi, CPLEX) to guarantee optimality within reasonable computational timeframes. 
Medium-scale networks (100--400 facilities) benefit most from the proposed VNS, which stays within approximately 3.3\% of the best solution available from the exact solver in minutes rather than hours, 
offering a compelling balance between solution quality and computational efficiency. Large-scale international supply chains (exceeding 800 facilities) require metaheuristic 
approaches, as exact methods become computationally prohibitive due to memory constraints and exponential runtime growth. The transparency of our integer-based encoding and 
decoding algorithm facilitates integration with enterprise resource planning systems, enabling seamless deployment in existing operational infrastructures. Moreover, the solution 
quality achieved by the proposed VNS remains within acceptable bounds for strategic planning decisions, such as facility location, capacity allocation, and waste management 
infrastructure design. Implementation requires only standard computational resources, making the approach accessible to organizations without specialized high-performance computing capabilities.

% ----------------------------------------------------------------------------------------------------------------------
\section{Conclusion}
\label{sec:conclusion}

This study presented a comprehensive optimization framework for the pistachio supply chain network, addressing both forward product flows and the recovery of by-products. 
Our contributions are threefold. First, we specified in full the backward decoding algorithm that maps a priority chromosome onto a feasible transportation tree for this network,
including the reconciliation of the jointly produced outputs at processing centres; the priority-based representation itself is adopted from the network design literature rather than
proposed here, but its decoding, which is what prior work on this problem left implicit, is what determines both reproducibility and the constraint-handling mechanism. Second, we
developed four problem-specific neighborhood operators that exploit the active/inactive node structure inherent to capacity-constrained supply chains, demonstrating statistically
significant improvements over standard permutation operators. Third, we reported a systematic benchmark across seven instance scales under a shared set of algorithmic components,
which makes the observed differences attributable to the search strategy rather than to incidental implementation choices.

Comprehensive computational experiments benchmarked the proposed VNS against a Genetic Algorithm (GA), Iterated Local Search (ILS), and the exact solver Gurobi. 
The results reveal a clear performance stratification across instance sizes. For small instances ($N \le 30$), simple local search methods such as ILS are sufficient and highly efficient, 
often outperforming more sophisticated approaches. However, as problem complexity increases to medium-sized instances ($N \approx 100$), the proposed VNS becomes statistically superior 
to both GA and ILS, demonstrating its enhanced search capability. This advantage becomes even more pronounced in large-scale instances ($N \ge 800$), where exact methods become computationally 
intractable due to memory and time constraints. In these challenging scenarios, the proposed VNS demonstrates remarkable scalability, consistently identifying high-quality 
feasible solutions that significantly outperform the evolutionary approach, establishing VNS as a promising approach for practical, large-scale supply chain optimization.

Regarding future research, two primary directions emerge. First, further variations of the problem model should be explored, such as incorporating explicit closed-loop feedback loops. 
Second, and critically, there is a need to investigate new encoding methodologies specifically designed to enable 
matheuristics—hybrid approaches combining metaheuristics with exact solvers. The priority-based encoding used in this work is highly effective for pure metaheuristics, as it enables 
efficient local search through the solution space. However, integrating exact methods (such as branch-and-bound) into the VNS framework would require a reverse transformation: 
converting the decoded transportation tree back into the original MILP variable space. This reverse mapping is extremely challenging because the decoding process involves complex, non-linear 
decisions (such as the proportional allocation rules in processing centers and the greedy priority-based flow assignments) that cannot be trivially inverted. Developing alternative encoding 
schemes that preserve a bidirectional mapping between heuristic representations and mathematical programming formulations could unlock powerful hybrid decomposition strategies, enabling 
the VNS to identify promising regions while exact solvers refine solutions with provable bounds. Bridging this gap between heuristic flexibility and exact solver precision remains a vital 
frontier for solving such complex, large-scale supply chain problems.

The source code used in this study are openly available at: \url{https://github.com/raissagd/Pistachio_CLSC}.
% ----------------------------------------------------------------------------------------------------------------------
%% Bibliography
\bibliographystyle{elsarticle-harv}
\bibliography{references}

\end{document}